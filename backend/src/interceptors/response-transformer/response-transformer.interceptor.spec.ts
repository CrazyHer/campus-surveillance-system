import { ResponseTransformerInterceptor } from './response-transformer.interceptor';

describe('ResponseTransformerInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTransformerInterceptor()).toBeDefined();
  });
});
